# json.dump: Ghi dữ liệu vào trong file JSON

# with open('data.json', 'w') as file:
#   with open() được sử dụng để mở tệp
#   "data.json" là tên của file json sẽ được mở.
#   w là chế độ ghi(write)
#   file là tên biến chứa file json

# json.dump(anime.__dict__,file)
#   json.dump() là hàm viết dữ liệu vào file
#   anime.__dict__ là dữ liệu dưới dạng dictionary, chứa thuộc tính của đối tượng.

# QListWidget tạo một kiểu hiển thị theo kiểu danh sách, cho phép hiển thị danh sách đối tượng, thêm và xóa 
# các phần tử đối tượng trong danh sách đó